import path from 'path';
import root from '../root';

export const filesDir = path.join(root, 'public', 'products');
export const sliderDir = path.join(root, 'public', 'slider');
export const publicDir = path.join(root, 'public');

